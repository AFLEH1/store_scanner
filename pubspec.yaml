name: store_scanner
description: "Store Scanner Hybrid App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^3.5.5
  tflite_flutter: ^0.10.0
  sqflite: ^2.3.0
  path: ^1.8.3
  image_picker: ^1.0.7
  image: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mobilenet.tflite
